# Frivilligt, och bara för den som kör appen lokalt på Linux.
# Behövs inte på en server och behövs inte för att appen ska starta.

# Ritar skrivbordsikonen (installera.sh -> ikon.py).
# QR-koden behöver INTE detta — den skrivs som SVG.
Pillow>=10
